{% extends 'base.html.twig' %}

{% block body %}
    <table class="table table-bordered">
        <thead>
        <tr>
            <th>Id</th>
            <th>Name</th>
            <th>Description</th>
        </tr>
        </thead>
        <tbody>
            <tr>
                <td>{{ item.id }}</td>
                <td>{{ item.name }}</td>
                <td>{{ item.description }}</td>
            </tr>
        </tbody>
    </table>
    {#<form action="{{ path("edit_form",{"id": item.id}) }}" method="POST">#}
        {#<label for="item[name]">Name</label><input type="text" name="item[name]">#}
        {#<label for="item[description]">Description</label><input type="text" name="item[description]">#}
        {#<input type="hidden" name="item[_token]", value="{{ csrf_token("item") }}">#}
        {#<input type="submit" value="Edit">#}
    {#</form>#}
    {{ form_start(form, {attr: {novalidate: 'novalidate'}}) }}
        {#{{ form_label(form.name) }}#}
        {#{{ form_widget(form.name) }}#}
        {#{{ form_widget(form._token) }}#}
    {{ form_end(form) }}
{% endblock %}

